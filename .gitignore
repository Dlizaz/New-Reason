# Xóa file .env (hoặc .evn) khỏi bộ nhớ theo dõi của Git (không xóa file dưới máy bạn)
git rm --cached .env .evn 2>/dev/null || true

# Commit thay đổi này
git commit -m "Chann va xoa file env khoi git tracking"

# Đẩy code sạch lên GitHub
git push origin main
